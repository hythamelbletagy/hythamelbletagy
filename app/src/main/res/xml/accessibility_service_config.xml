<?xml version="1.0" encoding="utf-8"?>
<!--
  Events come from every app so the floating badge knows when you leave
  Instagram/Facebook. The service ignores other apps' events apart from noting
  which app is on screen. Scrolls are only counted in the packages listed in
  ScrollCounterService (Instagram, Facebook, Facebook Lite and common browsers).
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeViewScrolled|typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagIncludeNotImportantViews"
    android:canRetrieveWindowContent="true"
    android:description="@string/service_description"
    android:notificationTimeout="50" />
